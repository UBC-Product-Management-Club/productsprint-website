class ProjectData {
  constructor(img, isFinished, title, text, link) {
    this.img = img;
    this.isFinished = isFinished;
    this.title = title;
    this.text = text;
    this.link = link;
  }
}
